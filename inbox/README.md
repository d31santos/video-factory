# inbox/ — Mode A drop zone

Drop existing footage here (a talk, demo, podcast, screen recording). The pipeline
imports it into Descript for transcription, segments it, and repurposes it into scored
short clips (Mode A). Supported: .mp4, .mov, .m4v, .webm. One file = one run.
